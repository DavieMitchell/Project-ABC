// One place defining what colour macros are shown in — the Food card,
// meal entries, and the Report charts all import this. Per the pastel
// blue redesign, every macro now shares the same single accent colour
// rather than being individually colour-coded.
const BRAND_BLUE = '#497B98'

export const MACRO_COLORS = {
  calories: BRAND_BLUE,
  fat: BRAND_BLUE,
  carbs: BRAND_BLUE,
  protein: BRAND_BLUE
}
